mutation createReaction($input: ReactionCreationInput!) {
    createReaction(input: $input) {
        itemType
        itemId
        type
        id
        submitter {
            userId
            username
        }
        date
    }
}
