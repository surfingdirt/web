mutation forgotPassword($email: String!) {
    forgotPassword(email: $email) {
        status
        type
        message
        errors {
            username
            email
            accountKey
            password
        }
    }
}
